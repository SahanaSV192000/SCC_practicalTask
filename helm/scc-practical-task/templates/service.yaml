apiVersion: v1
kind: Service
metadata:
  name: {{ include "scc-practical-task.fullname" . }}
  labels:
    {{- include "scc-practical-task.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scc-practical-task.selectorLabels" . | nindent 4 }}
